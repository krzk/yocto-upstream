# Some testing/development distro

require conf/distro/poky.conf

DISTRO = "krzk"
DISTRO_NAME = "krzk development Linux"
DISTRO_VERSION = "1.0"

MAINTAINER = "krzk"

IMAGE_INSTALL_append = " python-grpcio-tools python-daemon python-pybluez"
IMAGE_INSTALL_append += " udevil"

DISTRO_FEATURES_append += " systemd"
