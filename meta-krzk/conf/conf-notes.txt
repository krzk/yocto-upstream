
### Shell environment set up for builds. ###

You can now run 'bitbake <target>'

Common targets for distro krzk are:
    core-image-minimal

For distro krzk-musl:
    DISTRO=krzk-musl bitbake core-image-minimal
